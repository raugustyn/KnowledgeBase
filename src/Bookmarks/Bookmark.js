class Bookmark {
  constructor (caption, minX, minY, maxX, maxY) {
      this.caption = caption;
      this.minX = minX;
      this.minY = minY;
      this.maxX = maxX;
      this.maxY = maxY;
    }
}

export default Bookmark;